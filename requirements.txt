# 妖精论坛 Windows 客户端 —— 运行期依赖
# 安装：python -m pip install -r requirements.txt

# GUI 框架
PyQt6==6.11.0

# HTTP（唯一出网库，负责 Cookie / 会话 / 流式下载）
requests==2.34.2

# Live2D 原生渲染（桌宠）
live2d-py==0.7.0.4
PyOpenGL==3.1.10
numpy==1.26.4

# 打包期额外依赖（仅开发时用，运行不需要）：
#   nuitka>=2.8.4        主打包后端（产出大量 .pyd/.dll，无 .pyc）
#   pyinstaller>=6.20    备用打包后端
#   pillow>=12.0         仅用于从 favicon 重新生成 icon.ico / 处理素材
