# Recommended for most uses
DATABASE_URL=postgresql://<user>:<password>@localhost:5432/<dbname>

# For uses requiring a connection without pgbouncer
DATABASE_URL_UNPOOLED=postgresql://<user>:<password>@localhost:5432/<dbname>

# Parameters for constructing your own connection string
PGHOST=<your-neon-host-pooler>
PGHOST_UNPOOLED=<your-neon-host>
PGUSER=<your-db-user>
PGDATABASE=<your-db-name>
PGPASSWORD=<your-db-password>

# Parameters for Vercel Postgres Templates
POSTGRES_URL=postgresql://<user>:<password>@<host>/<dbname>?channel_binding=require&sslmode=require
POSTGRES_URL_NON_POOLING=postgresql://<user>:<password>@<host>/<dbname>?channel_binding=require&sslmode=require
POSTGRES_USER=<your-db-user>
POSTGRES_HOST=<your-host>
POSTGRES_PASSWORD=<your-db-password>
POSTGRES_DATABASE=<your-db-name>
POSTGRES_URL_NO_SSL=postgresql://<user>:<password>@<host>/<dbname>
POSTGRES_PRISMA_URL=postgresql://<user>:<password>@<host>/<dbname>?channel_binding=require&connect_timeout=15&sslmode=require


# BLOB DataBase
avatar_WEBHOOK_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
<your-public-key>
-----END PUBLIC KEY-----"
avatar_STORE_ID=<your-blob-store-id>
avatar_READ_WRITE_TOKEN=vercel_blob_rw_<your-token>

# SMTP 邮件配置
SMTP_PASSWORD=<your-smtp-password>

# Flask 安全配置
SECRET_KEY=<your-secret-key>
FLASK_ENV=production
CORS_ORIGINS=https://yjlt.top,https://www.yjlt.top

# PWA Service Worker 版本号（升级时改这里并重启服务即可，无需改代码）
SW_VERSION=1.0.0
